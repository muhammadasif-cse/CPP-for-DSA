#include <bits/stdc++.h>
using namespace std;

int main()
{
    vector<int> v = {10, 20, 30, 40};
    // v.resize(2);
    // v.reserve(4);
    // v.pop_back();
    // v.insert(v.begin() + 3, 20);
    // v.erase(v.begin() + 2, v.end());

    cout << v[v.size() - 1];

    // for (auto value : v)
    // {
    //     cout << value << " ";
    // }
    return 0;
}